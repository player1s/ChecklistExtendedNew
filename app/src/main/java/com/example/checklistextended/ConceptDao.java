package com.example.checklistextended;

import androidx.room.Dao;
import androidx.room.Delete;
import androidx.room.Insert;
import androidx.room.Query;

import java.util.List;

@Dao

    public interface ConceptDao {/*
        @Query("SELECT * FROM ConceptEntity")
        List<conceptModel> getAll();

        @Query("SELECT * FROM ConceptEntity WHERE uid IN (:userIds)")
        List<conceptModel> loadAllByIds(int[] userIds);

        @Insert
        void insertAll(conceptModel... conceptModels);

    @Insert
    void insertSingleConcept(String uid,String name);

        @Delete
        void delete(conceptModel user);
   */ }

